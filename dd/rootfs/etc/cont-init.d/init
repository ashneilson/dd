#!/usr/bin/with-contenv bashio

# Registration is now handled per-hub by the dd service on startup (see
# services.d/dd/run). For each configured hub the service fetches the hub's stable base
# station ID, then loads existing credentials from /config or registers using the hub's
# share code if none exist yet. There is therefore nothing to do at init time.
bashio::log.info "dd add-on initializing; per-hub registration is handled by the service on startup."
